import { Template } from '@libs/docs'

export const template: Template = {
  title: 'Chart',
  usage: 'import { Chart } from \'@ngx-canvas/core\';\n\nconst shape = new Chart();',
  inputs: [],
  outputs: [],
  extends: '../shape/shape.json',
  description: 'This will add a chart to the canvas'
}