{
  "name": "draw",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "projects/draw/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/projects/draw"],
      "dependsOn": [],
      "options": {
        "parallel": false,
        "commands": [
          "rollup -c projects/draw/rollup.config.mjs",
          "node scripts/pack-dist.mjs projects/draw"
        ]
      }
    },
    "nx-release-publish": {
      "options": {
        "packageRoot": "dist/projects/draw"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": [
          "projects/draw/**/*.ts",
          "projects/draw/package.json"
        ]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "projects/draw/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    }
  },
  "tags": []
}
